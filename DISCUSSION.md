# HTML Scanner — 설계 논의 기록

> 목표: HTML을 파싱해 **XSS 취약점**과 **링크·리소스**를 찾아내는 보안 스캐너 (Go)

---

## 1. 파서를 어디까지 만들 것인가

**논의** — DOM 파서로 만들면 되는가?

**결론** — DOM은 대안이 아니라 **상위 계층**이다. 순서의 문제였음.

```
바이트 → ① Tokenizer → ② Tree Construction → DOM
```

**Tokenizer(①)만 먼저 만든다.** DOM(②)은 보류.

| 근거 | |
|---|---|
| 필요 충분 | 목표(XSS·링크·리소스)는 "태그 하나"만 봐도 판정 가능 |
| 낭비 없음 | DOM을 만들더라도 ①은 어차피 선행 필수 |
| 난이도 | ②에는 암묵적 태그 닫기, foster parenting 등 함정이 집중돼 있음 |

**나중에 DOM이 필요해지는 조건** — "`<form>` 안에 CSRF 토큰이 있는가" 같은 **구조 질의**가 요구될 때.

---

## 2. 관통하는 원칙: Parser Differential

> **내 토크나이저가 브라우저와 다르게 해석하는 지점 = 취약점을 놓치는 지점**

일반 파서는 "잘 파싱하면" 성공이지만, 보안 스캐너는 **브라우저와 동일하게** 해석해야 한다.
공격자는 정확히 그 틈을 노린다. 이 원칙이 이후 모든 결정의 기준이 되었다.

**따름정리** — 정규화는 토크나이저에서 끝낸다. 규칙(rule) 쪽에서 대소문자·공백을 매번 신경 쓰게 만들면 언젠가 한 곳을 빠뜨린다.

---

## 3. 결정 사항

### 3.1 구조

| 항목 | 결정 | 근거 |
|---|---|---|
| 패키지 | `tokenizer/` 별도 분리 | Go의 패키지 단위는 파일이 아니라 **디렉터리** |
| 인터페이스 | `Next()` 풀(pull) 모델 | 스트리밍 가능, 위에 트리 빌더를 얹을 여지를 남김 |
| 버퍼 | `[]byte` | 구조 문자가 전부 ASCII, UTF-8은 그대로 통과 |
| 테스트 | 테이블 주도 + `t.Run` | 파서는 케이스가 수십 개로 늘어나는 종류의 코드 |

### 3.2 파싱 규칙

| # | 논의 | 결정 |
|---|---|---|
| 1 | `<` 는 항상 태그인가 | **아니다.** 다음 글자가 영문자·`/`·`!`·`?` 일 때만 태그. `a < b`, `I <3 Go` 는 텍스트 |
| 2 | 태그 이름 대소문자 | **ASCII 소문자로 정규화.** `<ScRiPt>` 우회 차단 |
| 3 | `strings.ToLower` 사용 | **불가.** 유니코드를 건드려(켈빈 기호, 터키어 İ) 브라우저와 어긋남 |
| 4 | `<script>` 내부 | **원시(raw) 모드.** `</script` 만이 탈출구. `style`, `textarea`, `title`, `iframe` 등 동일 |
| 5 | 속성 값 저장 | 따옴표 종류(`Quote`)까지 보존 |
| 6 | 중복 속성 | 슬라이스로 **전부 보존**, 조회 시 **첫 번째** 반환 (브라우저 동작) |

**#1의 파급** — `<p>a < <img onerror=alert(1)></p>` 에서 `<img>` 토큰이 통째로 사라져 **탐지 자체가 불가능**했음.

**#5의 근거** — 따옴표 없는 값(`href=x`)은 공백 하나로 속성 추가가 가능해 XSS 관점에서 가장 위험. 파싱 후엔 복구 불가능한 정보.

**#6의 근거** — 맵에 넣으면 나중 값이 덮어써서 브라우저와 정반대가 된다.
`<a href="safe" href="javascript:...">` → 브라우저는 `safe`를 쓴다.

---

## 4. 측정 결과

`jnu_main.html` (163KB, 실제 페이지) 기준.

### 4.1 원시 모드 도입 효과 — 오탐 제거

`<script>` 내부의 JS 문자열(`$(...).append("<a class='...'>")`)과 주석 처리된 코드가 가짜 태그로 잡히고 있었다.

| | 이전 | 이후 | 차이 |
|---|---:|---:|---:|
| 시작 태그 총계 | 831 | **704** | −127 |
| div | 241 | 200 | −41 |
| a | 132 | 106 | −26 |
| img | 58 | 44 | −14 |
| option | 22 | 12 | −10 |

> **숫자가 줄어드는 것이 개선이다.** 스캐너에서는 더 많이 찾는 것보다 오탐을 줄이는 쪽이 훨씬 어렵고 중요하다. 오탐이 많으면 결국 아무도 보지 않는다.

### 4.2 속성 파싱 후 첫 탐지

```
속성 총계          1,422
인라인 핸들러       5  (onclick)
javascript: URL     1
외부 URL (href/src) 83
```

---

## 5. 의도적으로 미룬 것

> "알면서 미루는 것"과 "몰라서 빠뜨린 것"을 구분하는 것이 파서 작업의 핵심.

| 항목 | 현재 동작 | 비고 |
|---|---|---|
| 문자 참조 디코딩 | 미구현 | `&#106;avascript:` 우회에 취약. **별도 레이어로 분리**할 것 — 상태 기계에 섞지 않는다 |
| 주석 / DOCTYPE | `>` 까지 통째로 | 진짜 주석 규칙 필요 |
| `</>`, `</ b>` | 브라우저와 불일치 | 브라우저는 각각 무시 / 주석 처리. 주석 구현 시 함께 정리 |
| script 내부 `<!--` | 미구현 | script data escaped state. 실제 XSS 우회에 사용됨 |
| self-closing 플래그 | `/` 를 단순 무시 | 다음 단계 |
| 위치 정보 (Line/Col) | 미구현 | 리포팅에 필수. 나중에 추가하면 전면 수정이 되므로 조기 도입 필요 |
| DOM 트리 | 미착수 | 구조 질의가 필요해지는 시점에 `Next()` 위에 얹는다 |

---

## 6. 반복해서 확인하는 것

- **`pos`가 반드시 전진하는가** — 파서에서 무한 루프는 상시 위험. 새 상태를 추가할 때마다 모든 경로를 확인한다.
- **`go vet`** — 컴파일러가 잡지 않는 것(도달 불가 코드 등)을 잡는다.
- **`go test -timeout 10s`** — 무한 루프를 스택 덤프와 함께 잡는다. `Ctrl+C` 는 아무 정보도 남기지 않는다.
- **테스트 실패 시 첫 질문** — "코드가 틀렸나, 기대값이 낡았나?"
